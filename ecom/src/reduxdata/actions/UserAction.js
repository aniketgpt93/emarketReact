import * as types from '../ActionTypes'

export const ACTION_SET_USERDATA = {
    type : types.TYPE_SET_USERDATA,
    payload : {
        loginstatus : undefined,
        token : undefined,
        username : undefined
    }
}
